# Adaptive Filtering and Transformers: From Widrow's LMS to the Gradient Circuits of Attention
> *This paper is the result of the shared "struggles" of DeepSeek and Rinat Abdullin, born in a dialogue where every formula was tested for robustness, and every conclusion — for common sense.*
**Автор:** Abdullin Rinat & Deepsek
**Репозиторий:** [Adaptive-Filtering-and-Transformers](https://github.com/rekaasb/Adaptive-Filtering-and-Transformers)

---

## Аннотация (Abstract)

Modern Transformer architectures are often seen as a breakthrough in deep learning, but their mathematical core — gradient descent and adaptive weight adjustment — has deep roots in classical signal processing. This paper traces the evolution from Bernard Widrow's Least Mean Squares (LMS) algorithm to the attention mechanisms of Transformers.

We show that both systems share the same foundation: a quadratic error surface, gradient descent, and a correlation matrix that captures the structure of the input signal. The key difference lies not in principle, but in **space and scale**: Widrow worked with linear filters in the signal domain, while Transformers operate in high-dimensional latent spaces with nonlinear transformations.

This paper bridges the gap between adaptive filtering and modern AI, revealing a continuous line of evolution from classical signal processing to the transformer architecture.

**Keywords:** Adaptive Filtering, Widrow, LMS, Gradient Descent, Attention, Transformers, Correlation Matrix, Signal Processing.

---

## Содержание

1. [Введение](#введение)
2. [Базис Уидроу: поверхность ошибки и метод наименьших квадратов](#1-базис-уидроу-поверхность-ошибки-и-метод-наименьших-квадратов)
3. [Градиентный спуск и адаптация](#2-градиентный-спуск-и-адаптация)
4. [От адаптивных фильтров к нейросетям](#3-от-адаптивных-фильтров-к-нейросетям)
5. [Мост к трансформерам](#4-мост-к-трансформерам)
6. [Заключение](#заключение)
7. [Литература](#литература)

---

## Введение

Современные трансформеры часто представляют как прорыв в глубоком обучении, но их математическое ядро — градиентный спуск и адаптивная настройка весов — имеет глубокие корни в обработке сигналов. В 1960–1970-х годах Бернард Уидроу и его коллеги разработали алгоритм наименьших средних квадратов (LMS), который ввёл понятие адаптивной фильтрации: система, которая настраивает свои параметры, чтобы минимизировать ошибку между выходом и желаемым сигналом.

В этой статье мы показываем, что LMS-алгоритм и трансформер имеют одно и то же математическое основание: квадратичную поверхность ошибки, градиентный спуск и корреляционную матрицу. Разница между ними не в принципе, а в **пространстве и масштабе**: Уидроу работал с линейными фильтрами в пространстве сигналов, трансформеры — с нелинейными преобразованиями в пространстве скрытых признаков.

---

## 1. Базис Уидроу: поверхность ошибки и метод наименьших квадратов

В адаптивной фильтрации задача ставится так: есть входной сигнал \( X_k \), который мы подаём на фильтр с весами \( W \). На выходе фильтра мы получаем сигнал \( y_k = X_k^T W \). Мы хотим, чтобы этот выходной сигнал был как можно ближе к некоторому «желаемому» сигналу \( d_k \).

Ошибка на каждом шаге определяется как:

$$
\varepsilon_k = d_k - y_k = d_k - X_k^T W
$$

Среднеквадратичная ошибка (СКО) — это математическое ожидание квадрата ошибки:

$$
\xi = E[\varepsilon_k^2] = E[d_k^2] + W^T R W - 2 P^T W
$$

где:
- \( R = E[X_k X_k^T] \) — корреляционная матрица входного сигнала,
- \( P = E[X_k d_k] \) — вектор взаимной корреляции между входом и желаемым сигналом.

Эта функция \( \xi(W) \) — **квадратичная поверхность** (параболоид). Она имеет единственный минимум, который достигается при оптимальных весах:

$$
W^* = R^{-1} P
$$

Это и есть **критерий Уидроу-Хопфа**. Фильтр, настроенный таким образом, даёт минимальную ошибку в классе линейных фильтров.

---

## 2. Градиентный спуск и адаптация

Если мы не можем вычислить \( R \) и \( P \) заранее (потому что сигнал нестационарен или мы не знаем его статистику), мы можем приближаться к оптимуму итеративно, используя **градиентный спуск**.

Градиент СКО равен:

$$
\nabla \xi = 2 R W - 2 P
$$

Обновление весов выполняется по правилу:

$$
W_{k+1} = W_k - \mu \nabla \xi(W_k)
$$

где \( \mu \) — шаг адаптации (learning rate).

На практике, вместо точного градиента, мы часто используем его мгновенную оценку — это приводит к **алгоритму LMS** (Least Mean Squares):

$$
W_{k+1} = W_k + 2 \mu \varepsilon_k X_k
$$

Этот алгоритм не требует хранения всей истории сигнала, работает в реальном времени и сходится к оптимальному решению при достаточно малом шаге μ.

---

## 3. От адаптивных фильтров к нейросетям

Уравнение Уидроу:

$$
W^* = R^{-1} P
$$

— это аналитическое решение для линейной задачи. Но в нейросетях мы имеем дело с **нелинейными** преобразованиями: активации, softmax, многослойные структуры.

Тем не менее, **принцип остаётся тем же**:
1. Есть входной сигнал \( X \).
2. Есть целевой сигнал \( d \) (ground truth).
3. Есть функция потерь (аналог СКО).
4. Мы вычисляем градиент и спускаемся по нему.

Разница лишь в том, что в нейросетях:
- Поверхность потерь невыпуклая (много локальных минимумов).
- Градиенты вычисляются через цепное правило (backpropagation).
- Вместо матрицы \( R \) мы имеем матрицы \( X^T X \) или \( W_Q W_K^T \).

Таким образом, **нейросети — это обобщение адаптивных фильтров Уидроу на нелинейные и многослойные системы**.

---

## 4. Мост к трансформерам

В механизме внимания трансформера мы видим ту же структуру:
- \( Q = X W_Q \) — запросы (аналог входного сигнала).
- \( K = X W_K \) — ключи (аналог опорного сигнала).
- \( V = X W_V \) — значения (аналог желаемого сигнала).

Билинейная форма:

$$
X W_Q W_K^T X^T
$$

— это аналог корреляционного интеграла \( x^T \varphi^{-1} y \) или корреляционной матрицы \( R \).

Softmax выполняет роль нелинейной нормировки, а умножение на \( V \) — это взвешенное суммирование, которое заменяет линейную комбинацию \( X^T W \).

Таким образом, **внимание — это адаптивный фильтр**, который:
1. Вычисляет резонанс (корреляцию) между токенами.
2. Нормирует его (softmax).
3. Взвешивает значения \( V \).

Этот процесс аналогичен тому, как адаптивный фильтр Уидроу вычисляет взвешенную сумму входного сигнала и корректирует веса, чтобы минимизировать ошибку.

---

## Заключение

Мы показали, что адаптивная фильтрация Уидроу и механизм внимания трансформера основаны на одной и той же математической структуре: корреляционной матрице, градиентном спуске и взвешенном суммировании. 

Разница между ними не в принципе, а в **пространстве** и **масштабе**. Уидроу работал с линейными фильтрами в пространстве сигналов, трансформеры — с нелинейными преобразованиями в пространстве скрытых признаков.

Эта преемственность показывает, что глубокое обучение не изобрело новую математику, а **перенесло** классические принципы адаптивной фильтрации в новую вычислительную среду.

---

## Литература

1. **Уидроу Б., Стирнз С.** Адаптивная обработка сигналов. — М.: Радио и связь, 1989.

2. **Ширман Я.Д.** Теория и техника обработки радиолокационной информации на фоне помех. — М.: Советское радио, 1973.

3. **Колмогоров А.Н., Фомин С.В.** Элементы теории функций и функционального анализа. — М.: Наука, 1976.

---

## О репозитории

Этот репозиторий является частью трилогии, посвящённой математической конвергенции классической обработки сигналов и архитектуры Transformer:

1. **Радиолокация памяти Ширмана** — физика резонанса и оптимального приёма.
2. **Функциональный анализ памяти Фомина** — математические пространства и операторы.
3. **Резонанс смысла** — смысл как резонанс ядер модели и приёмника.
4. **Адаптивная фильтрация и трансформеры** — от LMS к градиентным контурам внимания.

---

**Лицензия:** Creative Commons Attribution-NonCommercial-ShareAlike (CC BY-NC-SA).
